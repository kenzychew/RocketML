apiVersion: v1
kind: Service
metadata:
  name: {{ include "rocketml.fullname" . }}
  labels:
    {{- include "rocketml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rocketml.selectorLabels" . | nindent 4 }}
